<!-- Copyright (c) Better11. All rights reserved. -->
<!-- Better11 System Enhancement Suite - DenseCheckboxGrid Control -->
<!-- Compact checkbox grid for tweaks/settings with WinUtil-inspired dense layout -->
<UserControl
    x:Class="Better11.App.Controls.DenseCheckboxGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Section Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock x:Name="HeaderText"
                       Style="{StaticResource B11SectionHeaderStyle}"/>
            <TextBlock x:Name="CountText"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{StaticResource B11TextTertiaryBrush}"
                       Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Select All / None bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
            <Button x:Name="SelectAllButton" Content="Select All"
                    Style="{StaticResource B11CompactButtonStyle}"
                    Click="SelectAll_Click"
                    Height="22" FontSize="11" Padding="8,2,8,2"
                    Margin="0,0,8,0"/>
            <Button x:Name="SelectNoneButton" Content="Select None"
                    Style="{StaticResource B11CompactButtonStyle}"
                    Click="SelectNone_Click"
                    Height="22" FontSize="11" Padding="8,2,8,2"/>
        </StackPanel>

        <!-- Checkbox Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater x:Name="CheckboxRepeater">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="220"
                                       MinItemHeight="24"
                                       MinRowSpacing="2"
                                       MinColumnSpacing="4"
                                       ItemsStretch="Fill"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Style="{StaticResource B11DenseCheckBoxStyle}"
                                  ToolTipService.ToolTip="{Binding Description}"/>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</UserControl>
