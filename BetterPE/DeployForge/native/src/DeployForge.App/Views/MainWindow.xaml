<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DeployForge.App.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeployForge.App.Views"
    xmlns:vm="using:DeployForge.App.ViewModels"
    Title="DeployForge"
    MinWidth="1200"
    MinHeight="800">

    <Grid>
        <NavigationView
            x:Name="NavView"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="True"
            IsSettingsVisible="False"
            OpenPaneLength="280"
            PaneDisplayMode="Left"
            SelectedItem="{x:Bind ViewModel.SelectedNavigationIndex, Mode=TwoWay}"
            SelectionChanged="NavView_SelectionChanged">
            
            <!-- Navigation Header -->
            <NavigationView.PaneHeader>
                <StackPanel Margin="12,8" Orientation="Horizontal">
                    <FontIcon 
                        FontFamily="Segoe MDL2 Assets" 
                        Glyph="&#xE90F;"
                        FontSize="28"
                        Foreground="{StaticResource DeployForgePrimaryBrush}"/>
                    <TextBlock 
                        Text="DeployForge" 
                        FontSize="20" 
                        FontWeight="SemiBold"
                        Margin="12,0,0,0"
                        VerticalAlignment="Center"/>
                </StackPanel>
            </NavigationView.PaneHeader>
            
            <!-- Navigation Items -->
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Welcome" Tag="Welcome">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Build" Tag="Build">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Profiles" Tag="Profiles">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE77B;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            
            <!-- Footer Items -->
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Settings" Tag="Settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            
            <!-- Content Frame -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Page Content -->
                <Frame x:Name="ContentFrame" Grid.Row="0"/>
                
                <!-- Status Bar -->
                <Border 
                    Grid.Row="1" 
                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="16,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Status Message -->
                        <TextBlock 
                            Grid.Column="0"
                            Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                            VerticalAlignment="Center"
                            Opacity="0.7"/>
                        
                        <!-- Mount Status -->
                        <StackPanel 
                            Grid.Column="1" 
                            Orientation="Horizontal"
                            Margin="16,0">
                            <Ellipse 
                                Width="8" 
                                Height="8"
                                Fill="{x:Bind GetMountStatusColor(ViewModel.IsImageMounted), Mode=OneWay}"
                                Margin="0,0,8,0"/>
                            <TextBlock 
                                Text="{x:Bind GetMountStatusText(ViewModel.IsImageMounted), Mode=OneWay}"
                                VerticalAlignment="Center"
                                Opacity="0.7"/>
                        </StackPanel>
                        
                        <!-- Image Info -->
                        <TextBlock 
                            Grid.Column="2"
                            Text="{x:Bind ViewModel.CurrentImagePath, Mode=OneWay}"
                            VerticalAlignment="Center"
                            Opacity="0.5"
                            MaxWidth="300"
                            TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </Border>
                
                <!-- Loading Overlay -->
                <Grid 
                    Grid.Row="0"
                    Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                    Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}">
                    <StackPanel 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                        <ProgressRing 
                            IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                            Width="48"
                            Height="48"/>
                        <TextBlock 
                            Text="{x:Bind ViewModel.BusyMessage, Mode=OneWay}"
                            HorizontalAlignment="Center"
                            Margin="0,16,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
